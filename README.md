What it does is you call the function before you import packages for your python program. 

It will then parse out the modules/packages/libraries that are in the file you calling the function from.

Then it will attempt to update pip

THen it will try to do an pip install on each package that is imported in the script.
